<Window x:Class="WpfApp9.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp9"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <Label Content="Режим просмотра" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,22,0,0" VerticalAlignment="Top" Height="36" Width="190" RenderTransformOrigin="0.5,0.5">
        </Label>
        <Button x:Name="AdminAccessButton" Content="Панель Управления"  FontSize="12" Background="#f44336" Foreground="White" Click="OpenAdminWindowButton_Click" HorizontalAlignment="Left" Margin="661,51,0,0" VerticalAlignment="Top" Height="31" Width="118"/>
        <TabControl Margin="10,110,10,10">
            <TabItem Header="Category">
                <DataGrid x:Name="CategoriesTabGrid" 
                          AutoGenerateColumns="True" 
                          IsReadOnly="True"
                          Margin="5,0,5,0" Height="246"/>
            </TabItem>
            <TabItem Header="Equipment">
                <DataGrid x:Name="EquipmentTabGrid" 
          AutoGenerateColumns="True" 
          IsReadOnly="True"
          Margin="5"/>
            </TabItem>
            <TabItem x:Name="EmployeesTabItem" Header="Employees">
                <DataGrid x:Name="EmployeesTabGrid" 
               AutoGenerateColumns="True" 
               IsReadOnly="True"
               Margin="5"/>
            </TabItem>
            <TabItem Header="WarehouseStock">
                <DataGrid x:Name="WarehouseStockTabGrid" 
                 AutoGenerateColumns="True" 
                 IsReadOnly="True"
                 Margin="5"/>
            </TabItem>
        </TabControl>
        <Button Content="Редктировать Профиль" Click="OpenProfileButton_Click" HorizontalAlignment="Left" Margin="10,74,0,0" VerticalAlignment="Top" Height="31" Width="150" Background="#2196F3" Foreground="White" FontSize="12"/>
        <Button Content="Профиль" Click="OpenMyMovementsButton_Click" HorizontalAlignment="Left" Background="#2196F3" Foreground="White" Margin="10,40,0,0" VerticalAlignment="Top" Height="31" Width="81"/>
        <Button x:Name="WorkConfirmationButton" Content="Подтвирждение работ" HorizontalAlignment="Left" Margin="639,87,0,0" VerticalAlignment="Top" Width="140" Height="31" Click="Button_Click"  FontSize="12" Background="#f44336" Foreground="White"/>

    </Grid>
</Window>
